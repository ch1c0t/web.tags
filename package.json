{
  "name": "tags",
  "main": "lib/tags.js",
  "version": "0.0.0",
  "scripts": {
    "dev": "coffee --watch --compile --output lib src"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1"
  }
}
